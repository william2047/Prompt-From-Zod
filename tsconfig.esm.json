{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "module": "nodenext", // ESM output
        "moduleResolution": "nodenext", // Ensure compatibility with Node.js ESM
        "outDir": "./dist/esm" // Output directory for ESM build
    }
}